{{- if .Values.pki.enabled }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: swim-selfsigned-issuer
  labels:
    {{- include "swim-infra.labels" . | nindent 4 }}
spec:
  selfSigned: {}
{{- end }}
